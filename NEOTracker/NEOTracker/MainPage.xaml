<Page
    x:Class="NEOTracker.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NEOTracker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d">
    <Page.Resources>
        <DataTemplate x:Key="HamburgerMenuItem" x:DataType="toolkit:HamburgerMenuGlyphItem">
            <Grid Width="240" Height="48">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="48" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Image Grid.Column="0" 
                          Margin="12,12,11,12" 
                          Source="{Binding Glyph}" />
                <TextBlock Grid.Column="1" 
                           Text="{x:Bind Label}" 
                           Foreground="White" 
                           FontSize="16" 
                           VerticalAlignment="Center" />
            </Grid>
        </DataTemplate>

    </Page.Resources>
    

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <toolkit:HamburgerMenu OpenPaneLength="240"
                               DisplayMode="CompactOverlay"
                               CompactPaneLength="48"
                               HamburgerHeight="48"
                               HamburgerVisibility="Visible"
                               IsPaneOpen="False"
                               ItemTemplate="{StaticResource HamburgerMenuItem}">

            <toolkit:HamburgerMenu.ItemsSource>
                <toolkit:HamburgerMenuItemCollection>
                    <toolkit:HamburgerMenuGlyphItem Label="Visualization" Glyph="/Assets/icons/noun_900888_cc.svg"/>
                </toolkit:HamburgerMenuItemCollection>
            </toolkit:HamburgerMenu.ItemsSource>

            <Grid>
                <Frame x:Name="MainFrame"></Frame>
            </Grid>

        </toolkit:HamburgerMenu>
    </Grid>
</Page>
